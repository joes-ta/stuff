/* This Code is to be implemented as a system to save game data */

#include <ws2tcpip.h>
#include <stdio.h>
#include <iostream>
#include <fstream>
#include <vector>

using std::cout; using std::cerr;
using std::endl; using std::string;
using std::ifstream; using std::vector;

int main()
{
	string username="jpatric5"
	string filename("input.txt");
	vector<string> lines;
	string line;
	
	ifstream input_file(username)
	
}